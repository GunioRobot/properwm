pkgname=properwm
pkgver=0.8
pkgrel=1
pkgdesc="A real window manager."
arch=('i686' 'x86_64')
url=""
license=()
groups=()
depends=()
source=()
md5sums=()

build() {
  cd $startdir
  mkdir -p $pkgdir/usr/bin
  make
  cp properwm $pkgdir/usr/bin/
}
